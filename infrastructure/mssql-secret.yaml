apiVersion: v1
kind: Secret
metadata:
  name: mssql-secret
  namespace: infrastructure
  labels:
    app.kubernetes.io/name: mssql
    app.kubernetes.io/part-of: eregulations
type: Opaque
stringData:
  # IMPORTANT: Replace this value before applying.
  # Must meet SQL Server complexity requirements (min 8 chars, mixed case, numbers, symbols).
  sa-password: "CHANGE_ME"
