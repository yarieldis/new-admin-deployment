apiVersion: v1
kind: Service
metadata:
  name: mssql
  namespace: infrastructure
  labels:
    app.kubernetes.io/name: mssql
    app.kubernetes.io/part-of: eregulations
spec:
  type: ClusterIP
  ports:
    - port: 1433
      targetPort: 1433
      protocol: TCP
      name: sql
  selector:
    app.kubernetes.io/name: mssql
